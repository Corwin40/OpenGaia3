<table class="table">
    <thead>
    <tr>
        <th>Id</th>
        <th>Description</th>
        <th>Auteur</th>
        <th>actions</th>
    </tr>
    </thead>
    <tbody>
    {% for avi in avis %}
        <tr>
            <td>{{ avi.id }}</td>
            <td>{{ avi.description|raw }}</td>
            <td>{{ avi.author }}</td>
            <td>
                <a href="{{ path('op_gestapp_avis_edit', {'id': avi.id}) }}"><i class="far fa-eye"></i></a>&nbsp;
                <a href="#" class="js-modalSuppr text-danger" type="button" data-bs-toggle="modal" data-bs-target="#Suppr" data-bs-whatever="{{ avi.id }}">
                    <i class="fas fa-eraser"></i>
                </a>
            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="6">no records found</td>
        </tr>
    {% endfor %}
    </tbody>
</table>