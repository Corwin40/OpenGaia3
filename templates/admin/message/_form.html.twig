{{ form_start(form) }}
<div class="row">
    <div class="col-12">
        <div class="input-group input-group-sm">
            <span class="input-group-text" >Titre</span>
            {{ form_widget(form.subject, {'attr': {'class': 'form-control form-control-sm', 'placeholder':''}}) }}
        </div>
        <div class="input-group input-group-sm">
            <span class="input-group-text" >Destinataire</span>
            {{ form_widget(form.recipient, {'attr': {'class': 'form-select form-select-sm'}}) }}
        </div>
        <div class="row mb-1 mt-1 g-1">
            <div class="col-12">
                {{ form_widget(form.content, {'attr': {'class': 'form-control form-control-sm', 'placeholder':''}}) }}
            </div>
        </div>
    </div>
</div>
{{ form_end(form) }}
