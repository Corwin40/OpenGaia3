<form method="post" action="{{ path('admin_parrainage_delete', {'id': parrainage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parrainage.id) }}">
    <button class="btn">Delete</button>
</form>
